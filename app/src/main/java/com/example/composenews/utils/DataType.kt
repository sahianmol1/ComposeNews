package com.example.composenews.utils

enum class DataType {
    TOPHEADLINES,
    SOURCES,
}

enum class ListType {
    LIST,
    GRID,
    STAGGERED
}

enum class SortBy {
    popularity,
    publishedAt
}